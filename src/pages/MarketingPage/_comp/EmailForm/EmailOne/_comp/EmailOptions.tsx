import FontSizeProgress from './FontSizeProgress';
import AlignmentToggle from './AlignmentToggle';
import TextStyleToggle from './TextStyleToggle';

const EmailOptions = () => {
	return (
		// <div className=' bg-white rounded-lg shadow-md mr-4 flex flex-col'>
		// 	<div className='p-5 border-b text-center'>
		// 		<h3 className='text-xl'>EmailOptions</h3>
		// 	</div>
		// 	<div className='p-6'>
		// 		<p className='font-semibold my-3'>Style</p>

		// 		<div className='flex flex-col space-y-7'>
		// 			<FontSizeProgress
		// 				value={emailOptions.fontSize}
		// 				onChange={handleFontSizeChange}
		// 				min={10}
		// 				max={36}
		// 			/>

		// 			<TextStyleToggle
		// 				isBold={emailOptions.isBold}
		// 				isItalic={emailOptions.isItalic}
		// 				onBoldToggle={handleBoldToggle}
		// 				onItalicToggle={handleItalicToggle}
		// 				onLinkInsert={handleLinkInsert}
		// 			/>

		// 			<label className='mb-4 flex w-full justify-between items-center'>
		// 				<input
		// 					type='color'
		// 					name='textColor'
		// 					value={emailOptions.textColor}
		// 					onChange={handleChange}
		// 					className='mt-1 block w-1/6'
		// 				/>
		// 				<p className='block text-sm font-medium text-gray-700 w-full ms-1'>Text Color:</p>
		// 			</label>
		// 		</div>
		// 	</div>

		// 	<div className='border-t py-3 p-6'>
		// 		<p className='font-semibold my-3'>Section</p>

		// 		<div className='flex flex-col space-y-7'>
		// 			<AlignmentToggle
		// 				alignment={emailOptions.alignment}
		// 				onAlignmentChange={handleAlignmentChange}
		// 			/>

		// 			<label className='mb-4 flex w-full justify-between items-center'>
		// 				<input
		// 					type='color'
		// 					name='bgColor'
		// 					value={emailOptions.bgColor}
		// 					onChange={handleChange}
		// 					className='mt-1 block w-1/6'
		// 				/>
		// 				<p className='block text-sm font-medium text-gray-700 w-full ms-1'>Background</p>
		// 			</label>
		// 		</div>
		// 	</div>
		// </div>
		<></>
	);
};

export default EmailOptions;
